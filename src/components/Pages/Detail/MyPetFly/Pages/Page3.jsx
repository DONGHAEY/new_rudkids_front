import styled from "styled-components";

export const Page3 = ({ isRender }) => {
  return (
    <div
      style={{
        width: "100%",
        height: "100%",
        display: "flex",
        flexDirection: "column",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <p
        style={{
          backgroundColor: "gold",
          position: "absolute",
          zIndex: 30,
        }}
      >
        엥 이제 되는건가.. ㅇㅇ.
      </p>
    </div>
  );
};
